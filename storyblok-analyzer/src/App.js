// Importing React library for building the UI
import React from "react";

// Importing the Analyzer component, which handles the main functionality
import Analyzer from "./Analyzer";

// Main App component that serves as the entry point of the application
function App() {
  // Rendering the Analyzer component
  return <Analyzer />;
}

// Exporting the App component as the default export
export default App;
